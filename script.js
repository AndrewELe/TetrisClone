const grid = document.querySelector('.grid');
let squares = Array.from(document.querySelectorAll('.board div'));
const width = 10;

class Shapes {
    constructor(lshape, zshape, tshape, oshape, ishape) 
}